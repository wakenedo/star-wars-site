import styled from 'styled-components'

export const PeopleTableContainer = styled.div`
    
     
`
export const PeopleTableH1 = styled.div`
    color: var(--yellow);
    padding: 1rem;
    font-weight: 300;
    margin: 0 auto;
    align-items: center;
    text-transform: uppercase;
    font-size: clamp(1.5rem, 10vw, 2.5rem);
    box-shadow: 1.5px 1.5px var(--yellow);
`

export const PeopleTableArray = styled.div`
    display: grid;
`

 