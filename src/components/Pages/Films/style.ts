import styled from 'styled-components'

export const FilmCoverContainer = styled.div`
    background: var(--background-light);
    width: 685px;
    height: 800px;
    margin-left: 1.5rem;
`

export const FilmCoverContainerTextP = styled.p`
    font-size: clamp(1rem, 10vw, 1.5rem);
    font-weight: 100;
    color : var(--text-body);
    margin: 0.5rem 1.5rem;
`

export const CrawlTextP = styled.p`
    font-size: clamp(1.95rem, 10vw, 1.75rem);
    font-weight: 100;
    color: var(--yellow);
    text-align: justify;
    margin: 2.5rem;
`

export const FilmPageContainer = styled.div`
`

export const FilmCoverImg = styled.img`
    width: 685px;
    height: 800px;
`