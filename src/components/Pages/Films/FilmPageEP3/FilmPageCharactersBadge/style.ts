import styled from "styled-components";

export const FilmPageCharacterBadgeTextContainer = styled.div`
    color: var(--text-body);
    background: var(--background-light);
    width: 175px;
    margin: 0.75rem;
    display: flex;
    flex-direction: column;
`

export const FilmPageCharacterName = styled.h4`
    font-size: clamp(1.25rem, 10vw, 1.25rem);
    margin: 0.5rem;
    font-weight: 500;
    padding: 0.15rem;
    color: var(--yellow);
    text-align: center;
`

